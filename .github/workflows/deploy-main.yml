name: Deploy main

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Run blue/green deploy script
        env:
          CONTROL_REPO_DIR: /home/angrymusic/projects/life-dashboard
        run: ./deployment/deploy-bluegreen.sh
